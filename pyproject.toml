# python および uv は Jquants MCP を利用する目的だけで利用している
[project]
name = "investsupporter"
version = "0.1.0"
description = "Jquants MCP を利用する目的で作成したプロジェクト、必要ないならば削除しても良い"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
